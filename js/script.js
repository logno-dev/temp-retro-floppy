const disk = document.getElementById("disk");
const shadow = document.getElementById("shadow");

disk.style.animation = "3s bounce infinite ease-in-out";
shadow.style.animation = "3s shadow infinite ease-in-out";
